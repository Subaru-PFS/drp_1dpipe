# **Template** python script used by pbs scheduler to produce python executor script
#

import sys
import subprocess
import json
import ssl

tasks = {tasks}

if __name__ == '__main__':
    # usage :
    # pbs_executor N : run task #N

    cmd = sys.argv[1]
    task_id, key = tasks[int(sys.argv[2]) - 1]

    p = subprocess.Popen(task)
    p.wait()

# Local Variables:
# mode: python
# End:
